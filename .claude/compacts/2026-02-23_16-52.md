# Compact: 2026-02-23 16:52

## Summary

Session focused on fixing Models page interactions and implementing independent models per provider (global vs tool-specific). Major database migration added to support tool isolation for model configurations.

## Key Decisions

1. **Independent Models Design**: Each provider (id + toolId combination) has its own independent models, not shared with global
   - Database migration `004_model_configs_tool_isolation`
   - Model ID format: `${providerId}:${toolId}:${modelId}`

2. **Provider Selection with Composite Key**: `selectedProviderId` + `selectedProviderToolId` to uniquely identify providers in UI
   - Global and tool-specific providers with same ID can coexist without confusion

3. **Cleaned up completed plan files**:
   - Deleted: `models-manual-config-refactor.md`, `tool-isolation-redesign-analysis.md`
   - Updated: `plans/README.md`

## Work Completed

### Database Migration

- `packages/core/src/model/Database.ts`: Added migration `004_model_configs_tool_isolation`
  - New column: `provider_tool_id TEXT NOT NULL DEFAULT 'global'`
  - Updated UNIQUE constraint: `(provider_id, provider_tool_id, model_id)`

### Core Model Manager Updates

- `packages/core/src/model/ModelManager.ts`:
  - All model methods now accept `toolId?: string` parameter
  - `listModels`, `getModels`, `getModel`, `addModel`, `updateModelDetails`, `deleteModel`, `setModelEnabled`, `setDefaultModel`, `getDefaultModel`
  - Model lookup uses composite key: `provider_id + provider_tool_id`

### IPC Layer Updates

- `packages/gui/electron/ipc/model.ts`: All model handlers pass `toolId`
- `packages/gui/electron/preload.ts`: All model APIs accept `toolId`
- `packages/gui/src/types/electron.d.ts`: Updated type definitions

### Store Updates

- `packages/gui/src/stores/modelStore.ts`:
  - Added `selectedProviderToolId: string | null`
  - Added `selectProviderWithToolId(id, toolId)` method
  - All model actions accept `toolId` parameter

### Component Updates

- `packages/gui/src/pages/Models.tsx`:
  - Uses composite key for provider selection
  - `selectedProvider` query: `p.id === selectedProviderId && (p.toolId || 'global') === (selectedProviderToolId || 'global')`

- `packages/gui/src/components/model/ProviderList.tsx`:
  - Added `selectedToolId` prop
  - Uses composite key for selection state
  - `onSelect(id, toolId)` callback

- `packages/gui/src/components/model/ProviderDetail.tsx`:
  - Passes `toolId` to all model operations and dialogs

- `packages/gui/src/components/model/AddModelDialog.tsx`:
  - Added `toolId?: string` prop
  - Passes to `addModel` call

- `packages/gui/src/components/model/ModelEditDialog.tsx`:
  - Added `toolId?: string` prop
  - Passes to `updateModelDetails` and `deleteModel` calls

### Bug Fixes

- Fixed `UsageDashboard.tsx`: `formatNumber` and `formatCurrency` handle undefined values
- Fixed Models page onSelect logic: clicking provider only selects, doesn't auto-set tool override
- Fixed "Use Global" button: only calls `clearToolOverride`, not `setProviderEnabled`

### Tests

- `packages/gui/src/__tests__/pages/Models.test.tsx`: 5 tests passing
  - Use Global button behavior tests
  - Provider selection tests (no API calls on click)
- Updated `packages/gui/src/__tests__/setup.ts`: Added full mock API for model/proxy/tray

## Pending/In-Progress

1. **Tool Detection**: Current system doesn't know which tool user is actively using
   - `toolId` is just a provider ownership label, not "current active tool"
   - Need design decision on how to determine current tool (manual select, auto-detect, or proxy-based routing)

2. **Phase 4 Testing**: Unit tests from master-plan still not started

## Context for Next Session

### Hybrid Tool Isolation Architecture

```
Provider = (id, toolId)
- id='openai', toolId='global' → Global OpenAI provider
- id='openai', toolId='cursor' → Cursor-specific override

Models = (providerId, providerToolId, modelId)
- Each provider scope has independent models
- No sharing between global and tool-specific

Priority: Tool override > Global config > Default
```

### Key Files

- `packages/core/src/model/Database.ts` - Migrations
- `packages/core/src/model/ModelManager.ts` - Core logic
- `packages/gui/src/stores/modelStore.ts` - State management
- `packages/gui/src/pages/Models.tsx` - Main UI
- `packages/gui/src/components/model/ProviderList.tsx` - Provider list
- `packages/gui/src/components/model/ProviderDetail.tsx` - Provider details

### Open Question

User asked: "你怎么知道我当前使用的是哪个 tools 呢？"

- System currently does NOT track active tool
- Options discussed: manual selection, auto-detection, proxy-based routing
- Waiting for user decision on approach

## Related Plans

- `.claude/plans/model-refactor-master-plan.md` - Long-term refactoring
- `.claude/plans/master-plan.md` - Overall project plan
